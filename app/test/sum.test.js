import Tweets from '@/components/cards/Tweets';
import { render } from 'react-dom';
import {describe,expect,to} from 'vitest'
describe('#sum', () => {
    render(<Tweets Image={[]} ImageAmount={5} Text={"porn"} key={8} />)
});
